import type { Theme } from '../types';

/** Solarized Dark — high-contrast warm dark palette (Ethan Schoonover). */
export const theme: Theme = {
	id: 'solarized-dark',
	label: 'Solarized Dark',
	description: 'Solarized dark — warm, low-blue-light palette.',
	defaultMode: 'dark',
};
