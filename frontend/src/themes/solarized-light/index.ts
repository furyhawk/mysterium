import type { Theme } from '../types';

/** Solarized Light — the light counterpart to Solarized Dark. */
export const theme: Theme = {
	id: 'solarized-light',
	label: 'Solarized Light',
	description: 'Solarized light — warm, paper-like palette.',
	defaultMode: 'light',
};
