import type { Theme } from '../types';

/** GitHub Dark — the classic Mysterium look. */
export const theme: Theme = {
	id: 'github-dark',
	label: 'GitHub Dark',
	description: 'Dark GitHub palette — the default Mysterium look.',
	defaultMode: 'dark',
};
