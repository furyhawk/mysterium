import type { Theme } from '../types';

/** GitHub Light — the light counterpart to the default theme. */
export const theme: Theme = {
	id: 'github-light',
	label: 'GitHub Light',
	description: 'Light GitHub palette.',
	defaultMode: 'light',
};
